#include <bits/stdc++.h>

using namespace std;

#define rep(i,a,b) for(int i=a;i<b;++i)
#define PUB push_back
#define PUF push_front
#define POB pop_back
#define POF pop_front
#define trace(x) cout<<"x="<<x<<endl
#define trace(x,y) cout<<"x="<<x<<" y="<<y<<endl
#define trace(x,y,z) cout<<"x="<<x<<" y="<<y<<" z="<<z<<endl
#define trace(w,x,y,z) cout<<"w="<<w<<" x="<<x<<" y="<<y<<" z="<<z<<endl

typedef long long ll;
typedef vector<int> vi;

int main()
{
	ios::sync_with_stdio(0);
	return 0;
}
